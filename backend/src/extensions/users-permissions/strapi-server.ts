// @ts-nocheck
/**
 * Extension del plugin users-permissions.
 *
 * El `qrToken` se genera automáticamente vía lifecycle `beforeCreate`
 * en `content-types/user/lifecycles.ts`. Esta extension no requiere
 * código extra por ahora — se mantiene como punto de extensión futuro.
 */
export default (plugin) => plugin;
